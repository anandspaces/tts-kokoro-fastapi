services:
  backend:
    build: .
    container_name: kokoro_backend
    ports:
      - "9009:8000"
    volumes:
      - ./data:/app/data
      - ./static:/app/static
      - ./.env:/app/.env
    environment:
      - MODEL_DEVICE=cpu
    restart: unless-stopped
    networks:
      - tts_network

networks:
  tts_network:
    driver: bridge
